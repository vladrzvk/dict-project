version: '3'

services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: blog_dict
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: